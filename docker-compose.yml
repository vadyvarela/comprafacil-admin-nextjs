version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_SITE_URL: "http://localhost:3000"
      HOSTNAME: "0.0.0.0"
    restart: unless-stopped
